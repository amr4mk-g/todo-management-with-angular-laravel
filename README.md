# Todo Management project, Using Angular and Laravel

The user can create a todo list, update anyone or delete it, and send the list to his email.

## Usage: backend

```bash
cd backend
npm install
php artisan migrate
php artisan serve
```

## Usage: Site

```bash
cd todos
npm install
ng serve
```
